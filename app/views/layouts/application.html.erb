<!DOCTYPE html>
<html>
  <head>
    <title>GreenApron</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <link href="https://fonts.googleapis.com/css?family=Radley|Roboto" rel="stylesheet">
    <% if signed_in? %>
    <script>
    window.currentUser = {
      "id": <%= current_user.id %>, "username": "<%= current_user.username %>"
    }
    </script>
      <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
